.dev
.env
.env.prod
.claude
# HuggingFace cache (persistent, shared by bioner + backend)
.cache/

# Local & trained model artifacts (folder created at runtime by the bioner volume)
bioner/models/

# devflow worktrees
.devflow
